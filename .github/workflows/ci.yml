name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: # Allow manual runs without merging to main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup
        with:
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend deps
        run: npm ci
        working-directory: backend

      - name: Generate Prisma client
        run: npx prisma generate
        working-directory: backend
        env:
          DATABASE_URL: postgresql://dummy:dummy@localhost:5432/dummy

      - name: Lint backend
        run: npm run lint
        working-directory: backend

      - name: Install frontend deps
        run: npm ci
        working-directory: frontend

      - name: Lint frontend
        run: npm run lint
        working-directory: frontend

  test-unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - uses: ./.github/actions/setup
        with:
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend deps
        run: npm ci
        working-directory: backend

      - name: Generate Prisma client
        run: npx prisma generate
        working-directory: backend
        env:
          DATABASE_URL: postgresql://dummy:dummy@localhost:5432/dummy

      - name: Backend unit tests with coverage
        run: npm run test:cov -- --runInBand
        working-directory: backend

      - name: Install frontend deps
        run: npm ci
        working-directory: frontend

      - name: Frontend unit tests with coverage
        run: npm run test:cov
        working-directory: frontend

      - name: Upload backend coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage/lcov.info
          flags: backend
          fail_ci_if_error: false

      - name: Upload frontend coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage/frontend/lcov.info
          flags: frontend
          fail_ci_if_error: false

  test-integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://test:test@localhost:5432/test_db
      NODE_ENV: test
    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup
        with:
          cache-dependency-path: backend/package-lock.json

      - name: Install backend deps
        run: npm ci
        working-directory: backend

      - name: Generate Prisma client
        run: npx prisma generate
        working-directory: backend

      - name: Run DB migrations
        run: npm run db:migrate:deploy
        working-directory: backend

      - name: Run integration tests
        run: npm run test:integration
        working-directory: backend

  security:
    name: Security
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup
        with:
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend deps
        run: npm ci
        working-directory: backend

      - name: Backend npm audit
        run: npm audit --audit-level=critical
        working-directory: backend

      - name: Install frontend deps
        run: npm ci
        working-directory: frontend

      - name: Frontend npm audit
        run: npm audit --audit-level=critical
        working-directory: frontend

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker image for scanning
        uses: docker/build-push-action@v7
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: false
          tags: doc-classifier-backend:ci
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trivy vulnerability scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: doc-classifier-backend:ci
          format: table
          exit-code: "1"
          ignore-unfixed: true
          vuln-type: os,library
          severity: HIGH,CRITICAL

  # ---------------------------------------------------------------------------
  # Deploy — runs migrations then deploys to Cloud Run (main branch only).
  # Following: https://www.prisma.io/docs/orm/prisma-client/deployment/deploy-database-changes-with-prisma-migrate#deploying-database-changes-using-github-actions
  # Migrations run BEFORE the new revision goes live (blue/green safe: deploy
  # only additive migrations per ADR-DATA-001).
  # ---------------------------------------------------------------------------
  # TODO Story 1.x — Enable when GCP credentials (WIF_PROVIDER, WIF_SERVICE_ACCOUNT,
  # DATABASE_URL_PROD, GCP_REGION, GCP_PROJECT_ID, CLOUD_RUN_SERVICE) are
  # configured as repository secrets.
  # deploy:
  #   name: Deploy to Cloud Run
  #   runs-on: ubuntu-latest
  #   needs: [lint, test-unit, test-integration, security]
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   permissions:
  #     contents: read
  #     id-token: write   # required for WIF
  #   steps:
  #     - uses: actions/checkout@v6
  #
  #     - uses: ./.github/actions/setup
  #       with:
  #         cache-dependency-path: backend/package-lock.json
  #
  #     - name: Install backend deps
  #       run: npm ci
  #       working-directory: backend
  #
  #     # Step 1 — Apply pending migrations to production DB BEFORE new revision.
  #     # See: https://www.prisma.io/docs/orm/prisma-client/deployment/deploy-database-changes-with-prisma-migrate
  #     - name: Run database migrations
  #       run: npx prisma migrate deploy
  #       working-directory: backend
  #       env:
  #         DATABASE_URL: ${{ secrets.DATABASE_URL_PROD }}
  #
  #     # Step 2 — Authenticate to GCP via Workload Identity Federation (no JSON key).
  #     - name: Authenticate to GCP
  #       uses: google-github-actions/auth@v3
  #       with:
  #         workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
  #         service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}
  #
  #     # Step 3 — Push image to Artifact Registry and deploy to Cloud Run.
  #     - name: Deploy to Cloud Run
  #       uses: google-github-actions/deploy-cloudrun@v3
  #       with:
  #         service: ${{ secrets.CLOUD_RUN_SERVICE }}
  #         region: ${{ secrets.GCP_REGION }}
  #         image: ${{ secrets.GCP_REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/backend/doc-classifier-backend:${{ github.sha }}

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [lint, test-unit, test-integration, security]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          # persist-credentials: false is required so that @semantic-release/git
          # uses RELEASE_TOKEN (set below) to push, not the default GITHUB_TOKEN bot.
          persist-credentials: false

      - uses: ./.github/actions/setup
        with:
          cache-dependency-path: package-lock.json

      - name: Install root dependencies
        run: npm ci

      - name: semantic-release
        env:
          # RELEASE_TOKEN: fine-grained PAT with Contents + Pull requests write
          # Required to bypass branch protection and push CHANGELOG.md + version bump
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: npx semantic-release
