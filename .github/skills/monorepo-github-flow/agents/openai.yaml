interface:
  display_name: "Monorepo GitHub Flow"
  short_description: "GitHub-only, diff-driven, and BMAD PR mechanics"
  default_prompt: "Use $monorepo-github-flow to resolve and resume any existing issue, linked PR, and local or remote branch before creating GitHub resources. Create only missing, authorized resources, never duplicates or unapproved follow-up issues."
