meta {
  name: 11 Refresh After Logout 401
  type: http
  seq: 11
  tags: [
    auth
    error
  ]
}

post {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{newRefreshToken}}
}

tests {
  test("returns 401 Unauthorized", function () {
    expect(res.status).to.equal(401);
  });
}
